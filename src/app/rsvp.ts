export class RSVP {
    private name: string;
    private number: number;
    private iscoming: boolean;

    constructor(name:string, number:number, iscoming:boolean){
        this.name = name;
        this.number = number;
        this.iscoming = iscoming;
    }
}
